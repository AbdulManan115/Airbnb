{
  "name": "usercrud",
  "private": true,
  "version": "1.0.0",
  "description": "Railpack-ready workspace that exposes the backend API located in ./backend",
  "workspaces": [
    "backend"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "npm run build --workspace backend",
    "start": "npm run start --workspace backend",
    "dev": "npm run dev --workspace backend"
  }
}

